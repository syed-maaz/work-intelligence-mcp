# G3-REAP — Progress & Handoff Notes — 2026-08-06

## What was built

| Artifact | Path | Purpose |
|---|---|---|
| Reap core module | `src/reap/reap.mjs` | Self-contained, importable, column-aware, unit-aware |
| CLI script | `scripts/reap-2026-08.mjs` | `--dry-run` default (read-only open), `--apply` to execute |
| Dev validation | `~/.wi-dev-g3-reap/data.db` | Prod schema backup (taken 13:16 by prior run; NOT re-taken) |
| Doc-lie report | `docs/planning/notes/g3-doclaws.md` | Ghost commands + divergent inventories |
| Hygiene report | `docs/planning/notes/g3-hygiene.md` | Bak/worktree cleanup + retention policy |
| Scenario report | `docs/planning/notes/g3-scenarios.md` | 15/15 PASS |
| .gitignore | `*.bak`, `data.db-wal`, `data.db-shm` added | |

## Run commands (orchestrator, on prod after review)

```bash
DATABASE_PATH=~/.work-intelligence-mcp/data.db node scripts/reap-2026-08.mjs            # dry-run, read-only
DATABASE_PATH=~/.work-intelligence-mcp/data.db node scripts/reap-2026-08.mjs --apply    # execute
```

Env knobs: `KEEP_UNREAD` (default 4 → unread <5), `STALE_DAYS` (default 21).

## What `--apply` changes on PROD

**T1 — hung children (9 rows):** `subagent_dispatches` where `status='running'` AND `dispatched_at < now-21d` (all 9 measured 27.8d old, ms units):
- `status`: `'running'` → `'failed'` (NOT `'aborted'` — prod CHECK constraint `status IN ('pending','running','succeeded','failed','timed_out')` rejects `'aborted'`; script detected via DDL, falls back; warning emitted)
- `error_text`: → `'reaped-2026-08-06-stale-running'` (no `reason` column exists; `error_text` is the notes column)
- `completed_at`: → now (no `reaped_at` column exists; `completed_at` is the terminal timestamp)
- Net: running 9 → 0, failed 137 → 146.

**T2 — proactive_queue (269 unread):**
- No `status`/`processed_at` columns exist → archive marker = `read_at` NULL → now.
- Archive 265 rows (269 − 4 newest kept), no deletes. Unread 269 → 4 (<5 ✓).

**No schema changes. No deletes. No prod writes in dry-run mode.**

## Deviations from mission brief (reality vs brief)

1. `'aborted'` impossible (CHECK constraint) → `'failed'` + reason string in `error_text` (documented loudly in script warnings + scenarios §Schema-adaptation).
2. No `reason`/`reaped_at`/`status`/`processed_at` columns → mapped to existing columns; script prefers the named columns when they appear.
3. `dispatched_at` is ms not seconds — unit-aware comparison added (naive seconds compare would have missed ALL 9).
4. Dev sandbox unread drifted 269 → 282 (earlier session applied then reset on the dev copy, clobbering 13 originally-read rows; code path identical).
5. Hygiene: mission brief "479MB x2 baks" → measured 8 baks / 4.1 GB; worktrees "19 GB" → measured 1.2 GB (678M offender). Reports use measured values.

## DoD status

- E2E: full scope E2E = dev-copy dry-run→apply→verify + re-apply idempotency: **PASSED**
- 15 scenarios: **15/15 PASS** (g3-scenarios.md)
- 5-agent review: pending orchestrator dispatch (Wave A)
- typecheck: PASS (tsc --noEmit, 0 errors). lint: PASS on owned files (oxlint, 0 warnings; 20 pre-existing warnings elsewhere untouched)
- Commits: `72ec3eb` (script), `8633d85` (graceful degrade + 15/15), `docs stage` (this commit)

## BLOCKED notes

None. Streaming interrupted twice mid-session; each resume verified state before proceeding (no work lost — uncommitted files were intact).
