---
sidebar_position: 3
title: Components Map
---

# Components Map

> Where each piece of code lives, what it does, and its current status. Generated by walking `src/` and verifying imports.
> **Last verified:** 2026-05-21 (post Sprint A partial — REFACTOR-001 phase 2.1).

Use this when you ask "which Jira connector is real?" or "is `outlook-calendar.ts` still used?". For the high-level layering, see [`architecture/index.md`](./index).

---

## src/server.ts — MCP stdio entry point

| Item | Status |
|---|---|
| `setupHandlers()` → `ListToolsRequestSchema` | ✅ active — single source of truth for which tools the MCP exposes |
| Brain tool handlers (5) | ✅ active — `handleBrainGetContext`, `handleBrainGetDecision`, `handleBrainVerifyClaim`, `handleBrainRecallMemory`, `handleBrainRecordOutcome` |
| `wi_*` tool handlers (17) | ✅ active — generated from `TOOL_MANIFEST` |
| Legacy tool handlers (10) | ✅ active but `[DEPRECATED — use wi_<x>]` prefix; removal scheduled Phase 74 |

## src/tools/ — MCP tool layer

| File | Purpose | Status |
|---|---|---|
| `manifest.ts` | **Single source of truth for 17 `wi_*` tools.** Consumed by `server.ts` and `src/openclaw/plugin/src/wi-tools.ts`. | ✅ active (ADR-025) |
| `brain-get-context.ts` | `get_context` — operational context, TTL-cached | ✅ active |
| `brain-get-decision.ts` | `get_decision` — structured decision with rationale | ✅ active |
| `brain-verify-claim.ts` | `verify_claim` — adapter-based fact verification | ✅ active |
| `brain-recall-memory.ts` | `recall_memory` — palace + brain_decisions retrieval | ✅ active |
| `brain-record-outcome.ts` | `record_outcome` — closes the learning loop | ✅ active |
| `topic-expert.ts` | `ask_topic_expert` — synthesizes Jira + Teams + Email + GitHub | ✅ active (deprecated alias `wi_topics`) |
| `jira-report.ts` | `get_jira_report` — sprint board AI analysis | ✅ active (deprecated alias `wi_jira_get`) |
| `teams-updates.ts` | `get_teams_updates` — FTS5 + meeting summaries | ✅ active (deprecated alias `wi_teams`) |
| `search-all.ts` | `search_all` — cross-source FTS | ✅ active (deprecated alias `wi_search`) |
| `search-messages.ts` | `search_messages` — basic FTS5 | ✅ active (deprecated) |
| `daily-summary.ts` | Dashboard hero summary | ✅ active |
| `digest.ts` | `get_daily_digest` | ✅ active (deprecated alias `wi_digest`) |
| `action-items.ts` | `get_action_items` | ✅ active (deprecated alias `wi_action_items`) |
| `configure-topic.ts` | `configure_topic` | ✅ active (deprecated alias `wi_topics`) |
| `topic-suggestions.ts` | `get_topic_suggestions` + `dismiss_topic_suggestion` | ✅ active (deprecated) |
| `notebook.ts` | Topic notebooks (LLM memory per topic) | ✅ active (EP-26) |
| `obsidian-export.ts` | Obsidian vault mirror | ✅ active (EP-27) |
| `relationship-detector.ts` | Cross-topic relationships | ✅ active (EP-39) |
| `context-ranker.ts` | Relevance ranking for AI context | ✅ active (EP-34) |
| `code-indexer.ts` | Multi-repo code graph builder | ✅ active (EP-43) |
| `identity-resolver.ts` · `profile-builder.ts` | Teammate identity + profiles | ✅ active (EP-45) |
| `saturn-board.ts` | Saturn-93 board adapter | ✅ active (EP-50) |
| `blast-radius-alert.ts` | Blast radius score for changes | ✅ active (EP-43) |
| `error-analyzer.ts` | Error log analyzer | ✅ active (EP-18) |
| `zod-to-json-schema.ts` | Utility — Zod → JSON Schema for MCP `inputSchema` | ✅ active (ADR-025) |
| `index.ts` | Barrel export | ✅ active |

## src/services/ — orchestration + AI

| File | Purpose | Status |
|---|---|---|
| `analyzer.ts` (2 k LOC) | All Anthropic API calls. Methods: `analyzeMeeting`, `detectActionItems`, `generateDigest`, `generateJiraSprintReport`, `answerTopicExpert`, `chatWithContext`, `buildNotebook`, `updateNotebook`, `generatePreBrief`, … | ✅ active. **Method signatures are stable** — callers must not break. |
| `sync.ts` | 15-min background loop. Steps: 1 fetch, 2 normalize, 3 dedup, 4a FTS, 4b embed, 4c palace enrich, 5 action items, 6 alerts. | ✅ active |
| `analysis-service.ts` | Orchestrates analyzer calls with `withRetry` + `max_tokens: 4096` | ✅ active (EP-31-5/7) |
| `change-watcher.ts` | 100 ms poll of `changes_log` table; dispatches typed events | ✅ active (EP-63) |
| `orchestrator-agent.ts` | Generic tool_use loop agent (max 10 iter) | ✅ active (EP-64) |
| `correlation-agent.ts` | Nightly KG traversal — "what's converging?" | ✅ active (EP-65) |
| `embedder.ts` | Ollama embeddings client | ✅ active (EP-37) |
| `claude-code-runner.ts` | Spawns Claude Code CLI for research | ✅ active (EP-67, ADR-021) |
| `link-extractor.ts` · `link-fetcher.ts` · `content-extractor.ts` | Link-aware ticket analysis (EP-66, ADR-019) | ✅ active |
| `notification.ts` | macOS notification dispatch | ✅ active (EP-16) |
| `config.ts` | ConfigManager — `~/.work-intelligence/config.json` | ✅ active |
| `README.md` | **Stale (Mar 17)** — predates most of the code in this folder. Cleanup queued. | ⚠ stale |

### src/services/brain/ — Unified Brain (ADR-024)

| File | Purpose | Status |
|---|---|---|
| `decision-engine.ts` | `get_decision` core — cache lookup, recall, AI call, verify, persist | ✅ active |
| `context-builder.ts` | TTL-cached operational context (sprint, stuck, calendar, noise) | ✅ active |
| `recall.ts` | RRF over palace search + brain_decisions + KG (4 rank lists) | ✅ active |
| `verify.ts` | Claim verification dispatcher; adapter pack in `verifiers/` | ✅ active |
| `verifiers/` | Per-claim adapters (GitHub MCP, Jira MCP, code grep, build logs) | ✅ active |
| `learn.ts` | `record_outcome` — confidence delta + palace write via MemoryEnricher | ✅ active |
| `budget.ts` | `brain_user_budget_ledger` — per-user daily $ and tool-call budget | ✅ active (Phase 72 added `bucket` column for tool-call accounting) |
| `proactive-scan.ts` | Scheduled noise/staleness scan feeding context-builder | ✅ active |
| `staleness-detectors.ts` | Stale-data heuristics surfaced via `wi_brain_context` | ✅ active |
| `action-cluster-detector.ts` | Action item clustering | ✅ active |
| `anthropic-tool-use.ts` | Wraps `client.beta.promptCaching.messages.create` with forced tool_use schema | ✅ active |

## src/routes/ — extracted HTTP route modules (REFACTOR-001)

> Phase 1 of `web-server.js` modularization. Each file exports a `RouteHandler[]` consumed by the dispatcher in `web-server.js`. Live status table + extraction checklist in `src/routes/README.md`.

| File | Purpose | Status |
|---|---|---|
| `_types.ts` | `RouteHandler`, `RouteContext`, `HttpMethod` types. `RouteContext` carries `db`, `palaceClient?`, `memoryEnricher?`, `anthropicApiKey?`, `analyzer?`. | ✅ active (ADR-026) |
| `_util.ts` | TS-native `json()` / `readBody()` / `parseBody()` (Zod) helpers | ✅ active |
| `brain.ts` | 3 routes: `POST /api/brain/learn` · `POST /api/brain/verify` · `POST /api/brain/recall` | ✅ shipped 2026-05-21 (opening pass) |
| `action-items.ts` | 2 routes: `GET /api/action-items` · `GET /api/action-items/pending-review` | ✅ shipped 2026-05-21 (Sprint A.2) |
| `topics.ts` | 2 routes: `GET /api/topics` · `GET /api/topics/health` | ✅ shipped 2026-05-21 (Sprint A.3 partial) |
| `digest.ts` | 2 routes: `GET /api/daily-summary` · `GET /api/digests` | ✅ shipped 2026-05-21 (Sprint A.4 partial) |
| **`pr.ts`** | **10 routes: full `/api/pr/*` family** (list, review, enrich, create, post-review, commits, watch GET/POST/DELETE, watched-summary) | **✅ shipped 2026-05-21 (Sprint A.1 followup)** |
| `README.md` | Migration status table + 7-step extraction checklist + per-family migration order | ✅ active |

**Migration progress as of 2026-05-21:** **19 of ~80 routes extracted (~835 LOC)** across 5 families. The remaining work — heavy brain/digest/topics routes (need helper extraction first), Jira (Sprint C, has `sprintMeta` cache), and the long tail — is listed in priority order in `src/routes/README.md`. Smoke coverage: 26-check bridge suite (was 13 before REFACTOR-001).

**Related extraction:** `src/intelligence/knowledge-enrichment.ts` (new, 2026-05-21) — `enrichKnowledgeFromResearch` moved out of `web-server.js`'s scope so both the legacy bridge and `pr.ts` share one source. 3 call sites now import from one TS module.

## src/intelligence/ — investigation + research + memory

| File | Purpose | Status |
|---|---|---|
| `investigation-orchestrator.ts` (1.1 k LOC) | 3-layer ReAct bug investigation engine | ✅ active (EP-55, ADR-013). Earlier draft of this doc reported "47 k LOC" — that was bytes, not lines. |
| `pattern-extractor.ts` | Saves bug patterns post-investigation | ✅ active (EP-56) |
| `regression-date-extractor.ts` | Extracts the date a regression appeared | ✅ active |
| `entity-extractor.ts` | Regex NER (query-time) | ✅ active (EP-59) |
| `palace-client.ts` | Persistent MCP child process — MemPalace stdio | ✅ active (EP-58, ADR-016 supersedes ADR-015) |
| `palace-seeder.ts` | One-time bootstrap of operations feature flags into KG | ✅ active (EP-57) |
| `memory-enricher.ts` | Universal sync→palace writer (5 wings) | ✅ active (EP-58) |
| `knowledge-indexer.ts` | Codebase knowledge extraction | ✅ active |
| `research-engine.ts` | Claude Code research orchestrator | ✅ active (EP-67, ADR-021) |
| `research-tools.ts` | Tool surface for `research-engine.ts` | ✅ active |
| `prompt-evolver.ts` · `prompt-evolution-jobs.ts` · `prompt-seeds.ts` | OPRO + TextGrad + A/B prompt evolution | ✅ active (EP-67) |
| `quality-scorer.ts` | Haiku auto-scoring of research outputs | ✅ active (EP-67) |
| `cost-gate.ts` | Pre-call gate for $0.30–$0.50 research invocations | ✅ active (flagged in ADR-REVIEW as REDUNDANCY-001 — to be replaced by budget ledger) |
| `ownership-map.ts` | File ownership lookup | ✅ active |
| `traversal-formatter.ts` | KG traversal output formatter | ✅ active (EP-59) |
| `tools/` (subfolder) | Investigation engine's tool definitions | ✅ active |

## src/connectors/ — data sources

**Active production connectors:**

| File | Purpose | Status |
|---|---|---|
| `browser-session.ts` | `BrowserSessionManager` — singleton Playwright + real Chrome (`launchPersistentContext` + Keychain cookies) | ✅ active (EP-1) |
| `jira-adapter.ts` | `JiraMcpAdapter` — primary path,  Jira MCP via SQLite OAuth | ✅ active (EP-46, ADR-006) |
| `jira-browser.ts` | `JiraBrowserConnector` — Playwright fallback when MCP fails | ✅ active (EP-4) |
| `teams-browser.ts` | `TeamsBrowserConnector` — low-level Teams page interactions; imported by `src/server.ts:45` | ✅ active |
| `teams-chats.ts` | `TeamsChatScraper` — chat list + message scraping | ✅ active (EP-2) |
| `teams-meetings.ts` | `TeamsMeetingsScraper` — inline Recap tab + Claude Haiku meeting analysis | ✅ active (EP-2) |
| `outlook-browser.ts` | Primary Outlook scraper (browser) | ✅ active (EP-3) |
| `outlook-watcher.ts` | `OutlookWatcher` — `fs.watch` on `HxStore.hxd` for instant unread detection. Dynamic import in `web-server.js:6131`. | ✅ active |
| `github-mcp-client.ts` | `GitHubMcpClient` — primary GitHub path | ✅ active (EP-44) |
| `github.ts` | `createGitHubConnector` — REST client used by `topic-expert.ts` (live GitHub search at query time) | ✅ active (limited — not the primary path) |
| `mcp-oauth-client.ts` | OAuth token lifecycle for  MCP servers (Jira, GitHub) | ✅ active (EP-46, ADR-007) |
| `mac-calendar.ts` | Mac Calendar (`Calendar.app` via JXA) — primary calendar source post-EP-51 | ✅ active (EP-25 + EP-51) |
| `types.ts` | `UnifiedMessage`, `DataSource`, `ConnectorError`, `AuthConfig` types | ✅ active |
| `index.ts` | Barrel — currently only re-exports `JiraConnector` + types | ⚠ thin |

**Candidates for removal — currently no production importers:**

| File | Purpose | Status | Evidence |
|---|---|---|---|
| `jira.ts` | `JiraConnector` — legacy REST-token Jira client | ⚠ orphan | Only imported by `connectors/index.ts`, `connectors/README.md` (doc), and `tests/connectors/jira.test.ts`. Production code uses `jira-adapter.ts` or `jira-browser.ts`. |
| `outlook-calendar.ts` | `OutlookCalendarConnector` — browser-scraped Outlook calendar (EP-25 original) | ⚠ orphan | Only referenced by its own definition file and `ep25-calendar-sync.md`. Superseded by `mac-calendar.ts`. |
| `README.md` | **Stale (Mar 17)** — uses `JiraConnector` examples | ⚠ stale | Predates EP-46 / ADR-006 |

**Removal action queued — see `.planning/ADR-REVIEW.md` REFACTOR-001 follow-up.**

## src/db/ — SQLite layer

| File | Purpose | Status |
|---|---|---|
| `schema.ts` | `CURRENT_SCHEMA_VERSION = 46`, 46 migrations | ✅ active |
| `connection.ts` | `getDatabase()` singleton (WAL mode) | ✅ active |
| `migrations/` | One file per migration (v45_brain_tables.ts, v46_…) | ✅ active |
| `queries/action-items.ts` · `calendar.ts` · `code-graph.ts` · `decisions.ts` · `digests.ts` · `investigation.ts` · `jira.ts` · `meeting-topics.ts` · `meetings.ts` · `messages.ts` · `notebooks.ts` · `questions.ts` · `research-cache.ts` · `research.ts` · `system.ts` · `teammates.ts` · `topics.ts` · `transitions.ts` · `web-cache.ts` | Domain-scoped query helpers (split from former 1047-line `queries.ts` in EP-31-4) | ✅ active |

## src/openclaw/ — Atlas plugin

| File | Purpose | Status |
|---|---|---|
| `plugin/src/wi-tools.ts` | Tool registration for the OpenClaw daemon — imports `TOOL_MANIFEST` from `src/tools/manifest.ts` | ✅ active (ADR-023 + ADR-025) |
| `plugin/openclaw.plugin.json` | Plugin manifest declaring hooks (`message_received`, `before_prompt_build`, `tool_result_persist`, `agent_end`, `gateway_start`) | ✅ active |
| `plugin/install.sh` | One-shot installer for the OpenClaw plugin dir | ✅ active |
| `plugin/README.md` | Plugin-side reference — usage examples per `wi_*` tool | ✅ active (DOCS-01 from Phase 72) |

## web-server.js (root) — HTTP bridge

| Component | Status |
|---|---|
| REST endpoints (~80) covering everything the web UI and Atlas need | ✅ active |
| SSE drain `GET /api/events` — proactive_queue → browser tabs | ✅ active (EP-61) |
| 24h `proactive_queue` eviction in SSE loop | ✅ active (EP-62) |
| **Origin-allow-list CORS + optional bearer auth** (OP-4 / A-1) | ✅ active — replaces `*`; `MCP_BRIDGE_TOKEN` env enables hard auth |
| **`startupDistFreshnessCheck()`** (Bundle C.1 / A-6) | ✅ active — refuses to start if `dist/` is older than `src/`; bypass with `SKIP_STALE_DIST_CHECK=1` |
| **Route dispatcher loop** — walks `EXTRACTED_ROUTES`; first match wins, then falls through to legacy if-chain | ✅ active (REFACTOR-001) |
| **`/api/agents/health`** + agents block on `/api/system-health` (OP-5 / A-2) | ✅ active — per-agent isolation registry |
| **`/api/prompts`** + **`POST /api/prompts/rollback`** (Bundle C.2 / A-9) | ✅ active — prompt version listing + atomic rollback |
| **`/api/brain/budget`** (Bundle C.4 / U-9) | ✅ active — daily call + token spend per user |
| Boot block: `OutlookWatcher`, `TeamsBadgeWatcher`, `ChangeWatcher`, `OrchestratorAgent`, `MeetingPrepAgent`, `CorrelationAgent` (UTC-gated), `PromptEvolution`, `ResearchCachePrune`. Each is wrapped with `withAgentTick` so failures are isolated and visible. | ✅ active |
| **~6300 LOC** (down from 6.4 k after REFACTOR-001 opening pass) | ⚠ modularization in progress |

## web/src/ — React UI

### Pages (12 routes, grouped in sidebar)

| Page | Sidebar group | Status |
|---|---|---|
| Dashboard | Daily | ✅ active — DailySummary hero w/ StaleBanner (U-14), NeedsAttention, Calendar, Meetings, MyIssues, Saturn, MyPRsWidget, AlertFeed |
| Action Items | Daily | ✅ active |
| Daily Digest | Daily | ✅ active |
| Topic Expert | Search | ✅ active — notebook + chat split |
| Search All | Search | ✅ active |
| Teams Updates | Search | ✅ active (EP-53) |
| Jira Report | Work | ✅ active — sprint board, 3-tab analysis, Sources tab |
| PR Review | Work | ✅ active (EP-54) — `window.confirm()` gate before posting reviews (U-5 / OP-2) |
| Teammates | Work | ✅ active (EP-45) |
| Topics | Work | ✅ active |
| Weekly Report | System | ✅ active |
| System Health | System | ✅ active |
| **NotFoundPage** | (catch-all) | ✅ active (U-7 / Bundle B.1) — real 404 with suggestions, replaces silent redirect |

### Shell components

| Component | File | Status |
|---|---|---|
| `AppShell` | `components/shell/AppShell.tsx` | ✅ active |
| `Sidebar` — 4 grouped sections (Daily / Search / Work / System) (U-1 / OP-3) | `components/shell/Sidebar.tsx` | ✅ active |
| `Topbar` — title, status chips, **budget chip** (U-9), **palace banner** (A-10), sync button, ⌘K | `components/shell/Topbar.tsx` | ✅ active |
| `CommandPalette` | `components/shell/CommandPalette.tsx` | ✅ active |
| `ChatPanel` (SSE proactive badge + streaming-decide stages) | `components/shell/ChatPanel.tsx` | ✅ active (EP-61, OP-7) |
| **`ErrorBoundary`** (U-8 / Bundle B.2) — wraps `<Routes>`, isolates page crashes | `components/shell/ErrorBoundary.tsx` | ✅ active |

### Shared components

| Component | File | Status |
|---|---|---|
| `ChatMessage` — markdown via `MD_COMPONENTS` (linkify), severity-coloured proactive badge | `components/shared/ChatMessage.tsx` | ✅ active (OP-6 + U-17) |
| `DecisionCard` — brain decision UI + **mark-as-success/failed/abandoned buttons** (U-18) | `components/brain/DecisionCard.tsx` | ✅ active |
| **`StaleBanner`** (U-14 / Bundle C.5) — inline + block variants for "showing cached version" warnings | `components/shared/StaleBanner.tsx` | ✅ active |
| `MarkdownPanel`, `DailySummarySection`, `ChatFeedPanel` — all use `MD_COMPONENTS` from `lib/linkify.tsx` (U-10 phase 1.5 / Bundle B.4) | various | ✅ active |
| `BrainStatus` / Decisions history | `components/brain/*` | ✅ active (ADR-024) |
| `InvestigationViewer` | `components/jira/*` | ✅ active (EP-55) |

### Libs

| File | Purpose | Status |
|---|---|---|
| `lib/api.ts` | Typed fetch client. `createPR` / `postPRReview` default `dry_run: true`; new `streamBrainDecide()` for SSE; new `brainLearn()` for U-18. | ✅ active |
| `lib/linkify.tsx` | `linkify()` + `linkifyChildren()` + `MD_COMPONENTS` — shared by every markdown surface | ✅ active (OP-6 + B.4) |
| `lib/utils.ts` | `cn`, `formatRelative`, … | ✅ active |

## tests/

| Folder | Status |
|---|---|
| `tests/connectors/` | Connector unit tests (incl. `jira.test.ts` against the orphan `JiraConnector`) | ⚠ partial |
| `tests/openclaw/operational-parity.test.ts` | Round-trip parity test for all `wi_*` tools — CI-blocking (Phase 72) | ✅ active |
| `tests/palace-client-integration.test.ts` | MemPalace integration smoke test | ✅ active |
| `tests/tools/zod-to-json-schema.test.ts` | Schema converter unit tests (Phase 72) | ✅ active |

## scripts/

| Script | Purpose | Status |
|---|---|---|
| `scripts/smoke-bridge.sh` | 16-check end-to-end smoke for the HTTP bridge: liveness, agents, CORS, preflight, PR dry-run, extracted brain routes, SSE decide pipeline | ✅ active (`npm run smoke:bridge`) |
| `scripts/smoke-ui.mjs` | Playwright UI smoke — sidebar IA + linkify rendering, with screenshots to `/tmp/ui-*.png` | ✅ active (`npm run smoke:ui`) |
| `scripts/smoke-ui-extras.mjs` | One-shot screenshots of 404 page, ErrorBoundary, etc. — manual visual aid | ✅ active |
| `scripts/mcp-setup.ts` | OAuth setup for  MCP servers (Jira / GitHub) | ✅ active (ADR-007) |
| `scripts/palace-rebuild.ts` | Replay all SQLite data into MemPalace | ✅ active (EP-58) |
| `scripts/check-palace.ts` | Diagnostic: Python env, mempalace version, ChromaDB status | ✅ active |

Smoke-test protocol documented at [`.claude/rules/smoke-tests.md`](https://github.com/your-org/work-intelligence-mcp/blob/main/.claude/rules/smoke-tests.md).

## Rule for keeping this doc honest

When you add a file under `src/`, add a row here in the same commit. When you delete one, remove its row. When you rename a connector, update its row. The file should always answer "is this in use?" within 30 seconds of reading it.
